# Robots.txt for Rabbit Holes — https://www.rabbitholes.garden
#
# All bots — including search engines and AI crawlers — are welcome to read
# the site. A single group keeps the rules identical for every crawler.

User-agent: *
Allow: /

# Keep crawlers out of the API, generated JSON data, error pages, and
# query-string variants (prevents parameter/duplicate-URL noise).
Disallow: /api/
Disallow: /cms
Disallow: /*.json$
Disallow: /404
Disallow: /*?*

Sitemap: https://www.rabbitholes.garden/sitemap.xml
